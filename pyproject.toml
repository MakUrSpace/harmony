[project]
name = "harmony"
version = "0.0.1"
requires-python = ">3.10"

[build-system]
requires = ["setuptools", "setuptools-scm"]

[tool.setuptools]
packages = ["harmony", "observer"]

[project.scripts]
harmony = "harmony.hexServer:main"
observer = "observer.observerServer:main"
